<!DOCTYPE html>
<html lang="fr">
<%- include('../../components/head.ejs', {
   title: 'Ergogene | Création utilisateur',
   styles: [ '/styles/admin.css' ]
}) %>
<body>
    <%- include('../../components/header.ejs') %>

    <a class="admin-panel" href="/users">Revenir à la liste des utilisateurs</a>
    <h1>Création d'un utilisateur</h1>

    <form action="/users/create" method="POST">
         <%- include('../../components/errors.ejs') %>

         <% if (isSuperAdmin) { %>
            <select name="fullaccess" id="role">
               <option value="admin">admin</option>
               <option value="user">utilisateur</option>   
            </select>
         <% } else { %>
            <input type="hidden" name="fullaccess" value="user" />
         <% } %>
         <div>
            <label for="lastname">Nom</label>
            <input
               type="text"
               id="lastname"
               name="lastname"
               required
               value="<%= (locals.formData && locals.formData.lastname) ? locals.formData.lastname : '' %>" /> 
         </div>
         <div>
            <label for="firstname">Prénom</label>
            <input
               type="text"
               id="firstname"
               name="firstname"
               required
               value="<%= (locals.formData && locals.formData.firstname) ? locals.formData.firstname : '' %>" /> 
         </div>
         <div>
            <label for="email">Email</label>
            <input
               type="email"
               id="email"
               name="email"
               required
               value="<%= (locals.formData && locals.formData.email) ? locals.formData.email : '' %>" /> 
         </div>
         <div>
            <label for="password">Mot de passe</label>
            <input type="password" id="password" name="password" required> 
         </div>
         <div>
            <label for="confirm">Confirmation Mot de passe</label>
            <input type="password" id="confirm" name="confirm" required> 
         </div>
         <button class="form-button">Créer</button>
    </form>

    <%- include('../../components/footer.ejs') %>  
    <script src="/scripts/main.js"></script>
</body>
</html>
